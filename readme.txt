compile in the same way that you have been compiling java programs(using javac command).
To execute Traffic with an applet viewer, you may also execute the HTML file shown earlier. For example, if the preceding HTML file is saved with
Traffic.java,then the following command line will run Traffic :
appletviewer Traffic.java